name: Publish Release (npm)

on:
  release:
    types: [published]

jobs:
  publish-catalog:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8 # v6.0.1

      - name: Setup Node
        uses: actions/setup-node@395ad3262231945c25e8478fd5baf05154b1d79f # v6.1.0
        with:
          node-version: 22.x
          cache: "npm"

      - name: Install Dependencies
        run: npm ci --ignore-scripts

      - name: Build
        run: npm run build

      - name: Publish to Catalog (Prerelease)
        env:
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CF_ACCOUNT_ID_PRE }}
          CLOUDFLARE_API_TOKEN: ${{ secrets.CF_API_TOKEN_PRE }}
          CF_KV_NAMESPACE_ID: ${{ secrets.CF_KV_NAMESPACE_ID_PRE }}
        run: node --experimental-strip-types scripts/sync-to-kv.ts
        if: github.event.release.prerelease

      - name: Publish to Catalog
        env:
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CF_ACCOUNT_ID }}
          CLOUDFLARE_API_TOKEN: ${{ secrets.CF_API_TOKEN }}
          CF_KV_NAMESPACE_ID: ${{ secrets.CF_KV_NAMESPACE_ID }}
        run: node --experimental-strip-types scripts/sync-to-kv.ts
        if: !github.event.release.prerelease
