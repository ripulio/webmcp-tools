{
  "$schema": "../../../schemas/toolMetadata.json",
  "id": "debug_get_dom",
  "description": "Extract DOM structure with filtering. Use selector to scope extraction. Returns element tree with attributes and truncation metadata.",
  "groupId": "debug",
  "filters": [
    {
      "type": "domain",
      "domains": ["*"]
    },
    {
      "type": "path",
      "paths": [".*"]
    }
  ]
}
