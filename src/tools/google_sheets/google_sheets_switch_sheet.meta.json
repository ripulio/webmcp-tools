{
  "$schema": "../../../schemas/toolMetadata.json",
  "id": "google_sheets_switch_sheet",
  "description": "Switch to a different sheet (tab) in the currently open spreadsheet",
  "groupId": "google_sheets",
  "filters": [
    {
      "type": "domain",
      "domains": ["docs.google.com"]
    },
    {
      "type": "path",
      "patterns": ["/spreadsheets/d/.*"]
    }
  ]
}
